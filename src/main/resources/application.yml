server:
  port: 8080

spring:
  application:
    name: ai-rag-demo

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ai_rag?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: cao666888

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 3000ms

  ai:
    deepseek:
      api-key: sk-4cd487fa91cb43b3bdce78ea8284e040
      chat:
        options:
          model: deepseek-chat
          temperature: 0.7

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.ai.demo: debug
